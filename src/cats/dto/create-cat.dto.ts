export class CreateCatDto {
 name: string;
 age: number;
 breed: string;
}
//from user end values -> targetId, email -> validate
//-> splitpage, pagemetadata, targetMetadata, combineTargetMetadata
//-> manipulate -> orgId, pageId, targetId -> combineTargetMetadata
//-> insert
